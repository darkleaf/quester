language: node_js
node_js:
  - stable
script:
  - npm run test
  - npm run build #TODO: убрать, когда сделаем тестирование в DOM
  - npm run lint
  - npm run stylelint
after_success:
  - npm run push-coverage
cache:
  directories:
    - node_modules